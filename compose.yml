services:

  app:
    build: .

    image: bigc-it_quiz_app:1.0

    container_name: BigC-IT_quiz
    ports:
      - "5000:1040"

    volumes:
      - .:/app

    environment:
      - FLASK_ENV=development 

    restart: no